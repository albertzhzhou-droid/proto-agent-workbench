# Next implementation cycle

> September 13 continuation: the interrupted frozen Structure and Design
> promotion assessments are complete (`build/PRECISION_FINAL_ACCEPTANCE_20260913.md`).
> New scientific refinement development changes the source and runtime and is
> not covered by those passes. Follow [Candidate refinement and scientific
> validation](molecular-refinement-stage.md) for the active implementation,
> actual complex-candidate evidence and the remaining full scientific/UI scope.

> September 12 active scope: complete complex organic and inorganic design,
> prompt-selected workflows, and all three interface classes before closing
> model promotion. The current implementation and open acceptance work are in
> [Design and interface stage](design-and-interface-stage.md). Earlier 72-case
> results remain observed regression evidence; neither model passed their
> abstention gate. The original plan requires complete promotion evidence, not
> an additional invented human model-promotion approval step.

The repaired source passed all 806 regression tests. The first Design run
retained 40/40 positive passes but ended after a user-confirmed interruption:
47/130 attempted cases, 45 completed rows, two errors and 83 unrun rows.
It does not count as a successful promotion repeat. The next full run,
`build/qwen-design-formal-20260912a-r1`, passed 130/130 first-attempt schemas,
40/40 positive workflows and 90/90 independently reviewed negative responses,
with a valid model/runtime identity bracket. It binds the earlier frozen source
`921019a9...`. The [visual upgrade](visual-workbench-stage.md) integrates V01–V07
and the later camera repair, with scoped code/interaction checks and actual
browser evidence for direct/model studies, all interfaces, exports, reopening
and revision binding. The prior full regression was intentionally interrupted
at 97% without a JUnit result; `visual-full-20260912b` subsequently passed all
809 tests in 574.48 seconds, with no skips, failures or errors.
New computation result UI and final source/package acceptance remain open.
Matched E2B comparison, candidate
repeats on the final source and the remaining original gates remain Priority 1.

September 13 UTC update: Design r1 at
`build/design-visual-final-20260912/qwen-r1` passed 130/130 first-attempt schemas,
40/40 positive workflows and 90/90 independently reviewed negative responses on
commit `0957a04c79f97b1e7341693f5d0013a4790e4620`. Subsequent UI defects required
history-race, original JSON numeric-token, exact mechanism-admission and local
interface-file export repairs, including malformed imported unit handling.
The old pass and malformed UI records remain retained; the repaired source has
only scoped checks and an in-progress actual export audit, not full acceptance.

Priority 1 now proceeds in this order: finish independent review of the repaired
source-preview exports, freeze the repaired source, complete full regression and
required static checks, rebuild and accept the actual package, then run the
original Structure/Design candidate repeats and matched E2B comparison with
unchanged cases, gates and before/after identities. Preserve all failed and
interrupted observations. The [visual stage record](visual-workbench-stage.md)
links the scoped 44 Python, 52 Design UI, 27 interface helper and 33 workflow
checks. Portable Windows distribution remains Priority 2 under the existing
[portable plan](portable-windows-plan.md).

The September 12 scope extension adds the [visual workbench stage](visual-workbench-stage.md) to Priority 1. Every implemented computation, candidate/product and result needs a visible workbench part. The stage includes complete interface outputs, sample-bound reaction playback, precise 3D/site inspection and exact calculation-to-viewport binding. It is active work, not a completed visual acceptance claim.

The September 5 product increment closes the bounded Structure Studio Next 1–5
implementation queue. The original architecture remains broader than this alpha.
Use these gates for the next cycle; do not infer publication, device operation
or unrestricted model autonomy from local acceptance.

| Priority | Work | Acceptance gate |
|---|---|---|
| 1 | Complex design, interface workflows and model promotion | Complete actual candidate generation and the electrode/electrolyte, catalyst/reactant and solid/liquid modules. Compare candidate-bound conditional responses only with declared parameters and matched conditions. Reject silent changes to scientific scope; retain failed model attempts. Freeze fresh independently authored and separately reviewed scenarios, compare preserved E2B weights with stronger candidates on identical inputs, repeat candidate evaluation, and complete runtime, provenance, parity, resource and package checks. Model abstention explanations also require independent semantic review. |
| 2 | Portable Windows distribution | Implement the [portable Windows plan](portable-windows-plan.md): choose and freeze distributable Python/scientific runtimes, inventory shipped files and notices, install on a clean machine, and test relocation, upgrades and rollback. Sign only with an owner-controlled identity. The plan is not distribution acceptance; current previews reference installed backends. |
| 3 | Broader finite-system profile | Define admitted elements, charge/multiplicity, geometry units, basis/method and convergence rules before enabling additional molecules. Add reference energies and explicit rejection/nonconvergence cases. Never relabel the water installation check as general quantum chemistry. |
| 4 | Surface research extension | Keep the current Cu(111) geometries revision-bound. Before energies, define slab relaxation, coverage, vacuum/convergence and reference-state rules; compare only under the same profile. Adsorption geometry alone is not adsorption energy or catalytic activity. |
| 5 | Storage and operational hardening | Add independently verified run bundles, explicit schema migrations, multi-process edit conflicts, cleanup/retention controls and clean restart tests. Decide whether stronger OS isolation is necessary before third-party workers. |
| 6 | Accessibility and interaction polish | Test keyboard-only navigation and 3D alternatives with users, high contrast, narrow screens and large structures under bounded functional tests. Maintain numerical tables alongside plots. No pressure testing is included in this increment. |

M10 XDL procedure interoperability and M11 device interoperability remain
optional and outside the MVP. They require separately scoped license, semantic,
authorization and operational reviews described in the original plan. No
laboratory or device actions are enabled or scheduled here.

The owner approved MIT. No remote push, public release, signing or automated
continuation is part of this local delivery. The pre-existing project baseline
and frozen implementation are preserved in local commit
`162bff37c41c15704e335a73fe7fd4c77593277a` on
`codex/design-interface-acceptance-20260911`. The reviewed source repairs are
preserved in child commit `925e8701a0e05d2ccf96f4c33cd4e4e9a137c5eb`, with the
unchanged 166-entry identity recorded in `build/design-source-checkpoint-20260911c.json`.
Author and committer were both
verified as `Albert_Zhou <albertzhzhou@gmail.com>`. Generated build evidence
remains local; committing the implementation does not establish model promotion.
