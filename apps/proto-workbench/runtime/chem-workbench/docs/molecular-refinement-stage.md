# Candidate refinement and scientific validation

This stage extends the retained Design/Structure workflows with candidate-bound
quantum refinement and evaluated-result parts. It remains active. Completion of
the earlier frozen model-promotion assessments does not validate these new source
files or the broader scientific predictions requested for the Workbench.

## Latest continuation outcome

### Reconnected native process and continuous observations

Checkpoint v10 rechecks the same governed native PID 42072 and all 288 unchanged
product files. At 01:20:26 UTC on September 14, the job remained running with no
committed gradient. The original 34-atom candidate has 16 recorded SCF iterations,
a native convergence statement, and has entered analytic-gradient calculations.
No completed full gradient, optimized minimum or chemical accuracy is established.

A separate read-only observer now retains independent snapshots every 60 seconds.
The page at `http://127.0.0.1:8881/` reads its cache every 15 seconds; pausing the
page leaves both native work and host observations running. Actual browser checks
cover update continuity, selection preservation, error/identity/terminal display
states and exact JSON/CSV downloads. The original failed JSON serialization and
each subsequent parser revision remain retained rather than rewritten.

The final isolated stage, `build/refinement-live-scf-crlf-stage-20260913/`, passed
69 Python tests and Ruff. Its server and browser assets are unchanged copies of
the predecessor that passed 24 Node tests. A native wording omission was corrected
by recognizing the exact complete CR/LF statement, with prefix/suffix negative
cases for special line separators. Actual browser downloads preserve line 349,
all 17 rows (SAD plus 16 iterations), original numeric shape and the view hash.
Current preview PID 18040 uses unified session 88478. Desktop viewport evidence
does not establish responsive or whole-product scientific acceptance.

Use `build/refinement-development-checkpoint-20260913-v10.json` and the latest
section of `build/REFINEMENT_WORKBENCH_CONTINUATION_20260913.md` for exact receipts,
current handles and continuation order. Keep product/scientific-runtime files
frozen until the existing job ends. Its actual terminal state, teardown and full
result must be inspected before a subsequent native run. Earlier Hessian,
optimizer and Structure pure tests remain preparation evidence; actual native
validation and fresh final-source model promotion are still outstanding.

### Formal Design refinement workbench

The v7 continuation adds production candidate preparation, a dedicated Design
workflow branch and the formal preparation/approval/status/result panel. Saved
Design studies can now be restored through a read-only history endpoint without
rewriting their original JSON. Committed scientific output and raw artifacts are
verified before visualization; changing candidates invalidates old client actions.
Source corruption does not prevent cancellation of an independently owned job.

The final combined gate passed 1157 tests (3 legacy live small-system tests
deselected), Ruff and strict mypy for 31 source files, with identical source and
fixture hashes before and after. Actual browser acceptance covered original-study
restoration, a missing-optimizer rejection, gradient preparation/approval, candidate
switching, page reload and submission. This does not establish a successful native
gradient, optimized minimum or chemical accuracy.

A new governed gradient attempt for the original 34-atom, 19-heavy-atom geometry
is recorded as job `4407635e5e4841138d8526ec01ee1e3b`. Its initial actual native
preflight passed with 967 spherical basis functions and the analytic route, and
the lifecycle entered native execution. The attempt has separate 21600-second
wall and 86400-second CPU ceilings; the old 7200-second timeout remains unchanged.
At checkpoint v7 the new job was active, with no completed full-gradient frame.
Keep product/registry/runtime source frozen while that job is active, and inspect
current owned state before resuming. Follow
`build/REFINEMENT_WORKBENCH_CONTINUATION_20260913.md` and
`build/refinement-development-checkpoint-20260913-v7.json` for exact references.
The full scientific, visual and model-promotion objective remains active.

### Governed v2 route continuation

The next continuation connects the retained Design/spec/contract artifacts to the
existing ExecutionStore approval and launch reservation. The new governed plan
rebuilds the original graph/source closure from actual bytes, binds an explicit
disk budget outside the five worker resource limits, and distinguishes readable
history from a current worker authorization. The worker receives an exclusive
one-use host launch sideband and an opaque approval binding. Raw approval tokens
remain in the host store.

Request v2 now has a first-party worker entry, a shared native importer and initial
and terminal runtime verification. Host evidence brackets the supervised launch;
final admission verifies actual retained call records and optimization events.
Unknown optimizer filenames, out-of-range iterations, preexisting optimization
records, missing native returns and unverified process-tree exit fail admission.
The monitored route retains scratch and partial logs. The legacy resource limits
remain unchanged.

An actual isolated Psi4 preflight on the original 34-atom, 19-heavy-atom geometry
passed both observations in 67.27 seconds. It checked installed imports, actual
options, explicit isotope defaults, functional/analytic routing and the resolved
967-function basis. No energy, gradient or optimizer evaluation was requested.
The controller verified zero remaining Job Object processes and unchanged bytes
for 39 loaded product sources. Its receipt is
`build/refinement-runtime-preflight-native-20260913/90a4c47accfe4ec285ba07b2726cfa1b/receipt.json`.

The final registered store/worker/evidence regression passed 996 tests in
286.96 seconds, Ruff and strict mypy for 26 source files. The exact source and
fixture hashes matched before and after. Three existing live small-system tests
were excluded from this final complex-refinement software gate; the first broader
attempt and its 300-second test-controller timeout remain separately retained.
The combined receipt is
`build/refinement-governed-route-integration-20260913/6b687bb8a76b4fd9ad1225fcce71fdc1/receipt.json`.
Continue from `build/REFINEMENT_GOVERNED_ROUTE_20260913.md` and development
checkpoint v6. Native full gradients, complete native optimization, minimum and
reference/error validation, evaluated result parts, broader reaction/interface
coverage and a fresh model promotion assessment remain required by the full goal.
The original run-2 timeout and raw SCF checkpoint remain unchanged.

### Product integration after the timeout

The isolated execution, optimizer, observer, recorder, plan/subject and evidence
modules have been migrated into the formal `chem_workbench.refinement_execution`
package with product imports and portable test fixtures. The first combined suite
passed 593 pure tests, Ruff and strict mypy; final regression evidence is tracked
in `build/refinement-product-integration-20260913/`. These checks do not establish
native execution or chemical accuracy.

The real supervisor now supports an explicit refinement disk budget, preserves
scratch on that monitored route and checks the owned Windows Job Object has zero
active processes before reporting tree exit. Actual Windows process membership
and termination were verified with a bounded nonchemical probe. Abnormal exit
retains available captured prefixes and a failure record; journal write failures
cannot claim verified integrity or replace the original supervisor exception.

At that migration checkpoint, host approval/registration, cross-process context
and the v2 worker route were still open. That historical state is retained in
`build/REFINEMENT_PRODUCT_INTEGRATION_20260913.md` and the v5 development checkpoint.
The governed v2 continuation above describes the subsequent changes. The module
migration itself started no replacement quantum calculation or model evaluation.

### Original run-2 terminal evidence

On September 13 at 20:35:44 UTC, the original run-2 reached its unchanged
7,200-second wall limit and ended with `timeout`. SCF had converged after 16
iterations, but the integral-direct analytic gradient did not return. No complete
gradient frame or `output.json` was retained. The terminal controller reported
unchanged worker/runtime identity; the independent terminal audit rechecked the
54 product Python/JSON files, worker/launcher, input, logs and stopped native PID.
See `build/refinement-d3bj-terminal-audit-20260913/run-2/receipt.json`.

A stable 74,828,536-byte native SCF checkpoint was copied and hash-checked before
scratch cleanup. It remains unopened and is not an accepted restart input.
The installed energy `restart_file` route repeats SCF using a READ guess; the
gradient route does not process that keyword, and a deserialized generic
wavefunction does not reconstruct the typed live SCF state required by
`ref_wfn`. The exact source/API review is
`build/refinement-scf-restart-review-20260913.md`. No replacement calculation was
started during this continuation.

New code remains in isolated `build/` staging: 56 pure Design-subject replay
checks, 27 worker-lifecycle checks, 48 host-inventory/evidence checks (including
three full lifecycle integrations), and 35 disk-budget checks passed. These
checks do not establish native execution or scientific accuracy. Actual host
approval/job reservation, cross-process context, runtime preflight, source
registration and Job Object integration are still required before the new route
is a product capability. The disk monitor is periodic observation, not an OS quota.

The terminal evidence page at `http://127.0.0.1:8878/` passed 14 pure checks and
actual browser acceptance: timeout and pre-terminal capture are distinct, SCF
rows 4 and 16 select correctly, source provenance opens, full-result actions stay
disabled, and reload retains the same source values with clean browser logs.
The real browser CSV was reopened and all 17 rows matched the raw printed tokens.
The receipt is `build/refinement-timeout-browser-acceptance-20260913/receipt.json`
(raw SHA256 `e3cee6e7e639702ec61109f30d35140f8d597f5631772684f4242e6bc50efda7`).
This is acceptance of a failure presentation, not of the full gradient. The
separate future-positive page has 32 pure checks and was not used for this run.
The terminal continuation handoff is
`build/refinement-development-checkpoint-20260913-v4.json`; older live-state notes
below are historical and must not be used to poll or restart the ended session.

## Current implementation and evidence

The new worker binds exact source-candidate coordinates, stable atom IDs, method,
basis files, installed runtime files, numerical options and supervised resource
limits. Each retained frame contains raw gradient input/result bytes and discrete
evaluation order. Optimization iterations have no physical time, and no result
currently certifies a minimum.

The September 13 complex-candidate development uses retained Design candidate
`organic_4e7c7a1515dbac54`, C15H15NO3, with 34 explicit atoms. Its geometry hash is
`sha256:55cb8dc3e440adb89cb63fb3a9dc7fb7895420d86f733fdbbc6991655a581a1b`.
No simple-compound installation check is accepted as evidence for this stage.

The installed Psi4 1.11 runtime lacks an analytic wB97X-V gradient. An earlier PK
attempt failed with a native write error. A DIRECT attempt exposed 193 planned
finite-difference geometries and was explicitly interrupted; its partial native
logs remain retained. The worker now requires analytic routing before dispatch.
These attempts do not become successful calculations after a source repair.

Version 2 specs introduce explicit default-isotope bindings without changing the
meaning of version 1 records. Each binding connects ordered atom IDs, atomic and
mass numbers, exact decimal masses, original geometry, runtime identity and an
observed artifact. Before computation the worker matches the binding against the
installed runtime's defaults. Both raw input and result retain the full arrays;
changed masses are rejected with exact decimal comparison. The actual native
serialization check passed on the retained 34-atom molecule, using a clearly
synthetic result container and zero electronic calculations.

A separately registered `psi4.wb97x_d3bj.def2_tzvppd.optimize.v1` descriptor retains
the V functional base and uses its published two-body D3(BJ) counterpart with VV10
disabled. It is not relabeled wB97X-V. The profile binds `s-dftd3`, s6=1,
s8=0.2641, a1=0, a2=5.4959, s9=0, spherical def2-TZVPPD, explicit DIRECT screening
and forced analytic gradients. Native preparation confirmed 967 spherical basis
functions and the actual derivative route and dispersion parameters. This is
preparation evidence, not validation of a full energy, gradient or optimized structure.
The scientific rationale and limitations are in the local review
`build/refinement-analytic-d3bj-profile-review-20260913.md`, based on the
[published counterpart study](https://doi.org/10.1021/acs.jctc.8b00842) and
[Psi4's supported interface](https://psicode.org/psi4manual/master/dftd3.html).

The isolated prefix now contains exact conda-forge builds of dftd3-python 1.6.0,
simple-dftd3 1.6.0 and five dependencies. All 74 pre-existing Conda JSON records
remained unchanged and `pip check` passed. The new runtime inventory contains
11,705 files; earlier runtime freezes remain historical evidence only.

Focused checks passed 350 tests, including isotope, named-profile and returned
geometry rejection cases. Returned coordinates are checked against the submitted
coordinates before a gradient enters the optimizer; an invalid subsequent result
retains the earlier valid frame in a sealed failure envelope. An installed
OptKing fixture completed three steps with synthetic forces
on the same complex geometry; it exposed requested SIMPLE becoming effective
LINDH. That fixture is software compatibility evidence.

The actual D3(BJ) bridge/native comparison subsequently passed on the same
34-atom input: both paths returned the dispersion contribution
-0.0775092927799016 hartree, with zero observed energy and gradient differences.
Both paths use the same installed library, so this is API parity, not an
independent scientific reference. The full analytic DFT gradient is running in
`build/refinement-d3bj-complex-gradient-20260913/run-2`; inspect its owned terminal
receipt or live process identity before resuming. Its bound source/runtime must
remain unchanged while active.

The separate reusable dispersion diagnostic part has 15 offline front-end checks
and actual browser acceptance for the retained positive values, all 34 exact
coordinate/gradient rows, 3D point mode, orthographic projection, atom selection,
distance measurement, reload and CSV/JSON download. Every downloaded coordinate
and gradient component matched the retained source; the complete downloaded JSON
matched the rehashed view. Browser console errors were absent. The native Bohr
table remains authoritative over the Angstrom rendering projection. This part
does not coerce the dispersion contribution into a full refinement worker result.
The refinement result part's earlier failed/zero-frame acceptance remains valid;
its full-gradient positive case is still required.

The additional SCF diagnostic part now has 20 Python and eight Node checks plus
actual browser/export acceptance. Its two curves and exact table display the
fixed 19:37:44 observation (SAD and iterations 1–9), with capture age and a visible
stale label. It never treats numerical SCF progress as a reaction trajectory.
Actual browser CSV download/reopen preserved every printed field of all ten rows.
The separate live worker had reached iteration 12 at 19:56:46 UTC and remained
active. Its full-gradient outcome is still required.

## Required remaining work

1. Complete a new full complex-candidate analytic gradient after the retained
   run-2 timeout and completed D3(BJ) bridge/native comparison, with a reviewed
   resource policy and independently validated restart support if used. Retain
   raw output and source/runtime brackets. Preserve
   dependency, method and failed-attempt records. A dispatcher count describes
   outer requests; nested dispersion attempts need their own observation before
   claiming exhaustive cost/attempt accounting.
   The nested observer, explicit optimizer policy and result evidence callback
   now live in the product package and participate in the registered v2 route.
   Synthetic integration tests cover their exact wiring. The actual native
   preflight verifies imports/options/basis without evaluating a gradient.
2. Verify the bound optimizer settings and retained accepted/rejected history
   during a complete native optimization on complex held-out candidates. The
   product loop reserves a final gradient and reevaluates the final accepted
   geometry; host admission checks original parameter and event bytes, frame
   attribution, force/displacement criteria and the final raw reevaluation.
   Native internal-coordinate gradient transformations remain recorded native
   observations; the validator does not independently replay that algorithm.
3. Implement bound Hessian/frequency calculations with accounted derivative work,
   stationary-point classification and independent reference/sensitivity studies.
   Convergence must not be used as proof of physical accuracy or a global minimum.
4. Connect the registered Design refinement route to WorkflowService, explicit
   approval/status/result endpoints, candidate result parts and prompt routing.
   Implement a separate complete Structure source verifier before admitting that
   branch. The initial governed registry has been rebuilt; any further covered
   code changes require another rebuild and source freeze. Verify the updated
   product and model workflows. Existing model passes belong to their earlier
   source freeze.
5. Complete the separately defined ionic, open-shell, inorganic, periodic cell,
   slab and interface profiles; calculate candidate-specific observables and
   reactions under declared conditions with appropriate references.
6. Provide real reaction trajectories and measured or computed optical data for
   progress and appearance views. Symbolic colors, aggregate ODE samples and
   geometry optimization steps do not establish atomistic dynamics or appearance.

Actual energy/gradient values and their raw precision can be preserved exactly.
Physical predictions still depend on method, basis, environment and model error;
acceptance must quantify the demonstrated observable and uncertainty. The full
requested organic/inorganic design, interface simulation and visual Workbench
scope remains required.

## Local continuation records

- `build/PRECISION_FINAL_ACCEPTANCE_20260913.md`: previous frozen promotion closeout.
- `build/refinement-development-checkpoint-20260913.json`: checkpoint preceding
  the mass-binding repair; it remains an immutable historical snapshot.
- `build/refinement-bound-isotopes-native-20260913/receipt.json`: native mass binding.
- `build/refinement-d3bj-complex-gradient-20260913/preparation.json`: new request
  and zero-calculation preparation; inspect its run subdirectory for actual jobs.
- `build/refinement-d3bj-final-focused-tests-20260913.log`: latest focused software checks.
- `build/refinement-d3bj-dispersion-parity-20260913/receipt.json`: actual dispersion API parity.
- `build/dispersion-diagnostics-browser-acceptance-20260913/export-verification.json`:
  actual browser-downloaded CSV and JSON verification.
- `build/REFINEMENT_D3BJ_CONTINUATION_20260913.md`: active process and staged next work.
- `build/refinement-product-integration-map-20260913.md`: remaining production hooks.
- `build/refinement-staging-validation-20260913-v4/receipt.json`: 251 pure staging
  checks and exact dependency hashes; the prior v3 failed orchestration is retained.
- `build/refinement-subject-plan-integration-20260913/handoff.json`: eight additional
  source-to-plan-to-request-v2 checks with unchanged coordinate/settings bytes.
- `build/refinement-recorder-staged-20260913/validation-final-v1/receipt.json`:
  34 pure recorder and loop-integration checks.
- `build/refinement-dispatch-staged-20260913/README.md`: observed transport stage,
  successful/failed raw-byte integration and outstanding live supervision.
- `build/refinement-core-staging-validation-20260913/e4f7d11e2c504f59b841341b6ad3a494/receipt.json`:
  final 351-test combined core staging pass, including 39 actual-byte evidence
  checks, with unchanged helper/test identities. This remains pure software evidence.
- `build/refinement-scf-progress-browser-acceptance-20260913/BROWSER_ACCEPTANCE.md`:
  actual SCF browser and exact CSV download/reopen verification.
- `build/refinement-observed-pipeline-integration-20260913/validation-4d72eb556b8f4e09bb6eb2521c6cae86/receipt.json`:
  nine additional pure tests across the actual staged recorder, transport, nested
  observer and execution/scientific validators. Single-gradient state remains
  incomplete even when its synthetic execution evidence is complete.
